# Production Environment Configuration
# Add these variables to your Render service environment

NODE_ENV=production
PORT=5000

# Database Configuration
MONGODB_URI=mongodb+srv://raomateen:Mateen.234.saeed@cluster0.8eerplv.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration - IMPORTANT: Generate a new secure secret for production
JWT_SECRET=your_super_secure_jwt_secret_here_change_this_in_production_minimum_32_characters_long
JWT_EXPIRE=30m
COOKIE_EXPIRE=30

# Frontend Configuration - Update with your actual frontend domain
CLIENT_URL=https://your-frontend-domain.onrender.com

# Optional: Additional Security Settings
BCRYPT_SALT_ROUNDS=12
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_TIME=15

# Note: To generate a secure JWT secret, run:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
